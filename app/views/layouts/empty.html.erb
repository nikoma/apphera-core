<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>EQBotics</title>
    <!-- Main javascript files -->
    <%= javascript_include_tag 'application' %>
    <!-- Include style per-controller - vendor plugins -->
    <%= stylesheet_link_tag params[:controller] if ::Rails.application.assets.find_asset("#{params[:controller]}.css") rescue nil %>
    <!-- Main css styles -->
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= csrf_meta_tags %>
  </head>
  <body class="gray-bg">
    <div class="newloginColumns animated fadeInDown">
      <div class="row">
        <%= render 'layouts/flash' %>
      </div>
    </div>
    <!-- Main view  -->
    <%= yield %>
    <!-- Include javascript per-controller - vendor plugins -->
    <%= javascript_include_tag params[:controller].split('/')[1] if ::Rails.application.assets.find_asset("#{params[:controller].split('/')[1]}.js") rescue nil %>
    <!-- Include javascript per-view -->
    <!-- For demo purpose we include javascript in view but you can easily start SeedProject and organize it with Rails asset pipeline as you want -->
    <%= yield :javascript %>
  </body>
</html>
